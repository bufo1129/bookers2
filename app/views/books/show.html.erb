<div>
  <%= image_tag @book.get_image %>
  <p>ユーザー名：<%= @book.user.name %></p>
  <%= link_to book_path(@book.id) do %>
  <p>title：<%= @book.title %></p>
  <% end %>
  <p>opinion：<%= @book.body %></p>
  <% if @book.user == current_user %>
    <%= link_to "Destroy", books_path(@book), method: :delete %>
  <% end %>
</div>